# RMS Memory 1.0.5 — Release Gate

Status: pre-release validation in progress.

## Local verification

- `cargo fmt -- --check` — passed
- `cargo test` — 48 passed
- `cargo clippy --all-targets -- -D warnings` — passed
- `git diff --check` — passed
- Release build — passed
- Installed binary — `/usr/local/bin/rms-memory` reports `rms-memory 1.0.5`
- Local binary was re-signed after installation for IDE launch compatibility.

## Dogfood

On `rms-memory-mcp`, two consecutive installed-binary `reindex --code` runs completed successfully:

1. Initial run: approximately 44 seconds, including embedding-model work.
2. Unchanged follow-up run: approximately 2.6 seconds.

The terminal capture did not include the CLI statistics line, so this artifact intentionally does not claim exact embedded/reused counts. The large timing reduction is consistent with the implemented content-hash embedding reuse path.

## Remaining release checks

- Exercise the updated MCP code-search tools through a live JSON-RPC session.
- Dogfood against a larger Rust workspace.
- Observe five IDE processes with `code_index_mode = "watch"` after rapid saves, then confirm idle CPU.
